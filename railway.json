{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn playto_pay.wsgi:application --bind 0.0.0.0:$PORT",
    "buildCommand": "bash render-build.sh",
    "restartPolicyType": "ON_FAILURE"
  }
}