{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "cd backend && python3 -m pip install -r requirements.txt --quiet && python3 -m gunicorn playto_pay.wsgi:application --bind 0.0.0.0:$PORT --workers 4 --timeout 120",
    "buildCommand": "bash render-build.sh",
    "restartPolicyType": "ON_FAILURE"
  }
}